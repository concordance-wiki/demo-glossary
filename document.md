---
aliases: [raw document, untyped resource]
---
# Document

A resource that no rule types: a markdown file left as-is, a slide deck, a Word file, a PDF, a transcript. A document is indexed and its words are recorded, but it enters the model only through what it documents. Its page shows a preview when one exists and the text extracted from it.
